 //instead of (+) we ca use (\) operator
let message1 = ' this is a long message '+' that spans accross multiple lines '+' in the code '
console.log(message1);// this is a long message that spans across multiple lines in the code 